source "https://rubygems.org"

# Matches GitHub Pages' build environment exactly, so what you see
# locally is what goes live. Run:  bundle install  then  bundle exec jekyll serve
gem "github-pages", group: :jekyll_plugins

gem "webrick", "~> 1.8"   # needed for local `jekyll serve` on Ruby 3+
